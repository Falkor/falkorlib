<%= ("=" * config[:width]) + "\n" %>
Welcome to <%= config[:hostname].chomp %>
<%= ("=" * config[:width]) + "\n" %>
    <%= ::Artii::Base.new().asciify( config[:title] ).split("\n").join("\n    ") %>
<% if config[:subtitle] %>
    <%= ::Artii::Base.new().asciify( config[:subtitle] ).split("\n").join("\n    ") %>
<% end %>
<%= ("=" * config[:width]) + "\n" %>
  Hostname: <%= config[:hostname].chomp %>
  OS:       <%= config[:os] %>
  IP:       <%= Facter.value(:ipaddress) %> (@mac: <%= Facter.value(:macaddress) %>)
  Support:  <%= config[:support] %>

 === <%= config[:desc] %> ===
 <%= Facter.value(:sp_machine_name) %> (<%= Facter.value(:sp_cpu_type) %> <%= Facter.value(:sp_current_processor_speed)%> <%= Facter.value(:sp_number_processors) %> cores)
 RAM: <%= Facter.value(:memorysize) %> -- Swap size: <%= Facter.value(:swapsize) %>

<%= ("=" * config[:width]) + "\n" %>
